# 需求接入规范

## 核心原则

所有需求在进入开发流程之前，**必须**完成标准化接入，不允许基于口头描述或非结构化文本直接开始开发。

## 需求来源分类

| 来源类型 | 典型形式 | 使用模板 |
|---|---|---|
| 内部研发自驱 | 技术优化、重构、新特性提案 | `from_prd.md` |
| 运营/业务方 | 飞书消息、口头描述、会议记录 | `from_verbal.md` |

## 必填字段（所有需求）

以下字段缺失时，**禁止**流转至技术设计阶段：

- `need_id`：需求唯一编号（格式：`REQ-YYYYMMDD-XXX`）
- `title`：一句话标题，不超过20字
- `background`：业务背景，回答"为什么要做"
- `goal`：可量化的目标，至少一条
- `scope`：明确的功能边界（包含什么、不包含什么）
- `priority`：P0 / P1 / P2（定义见下方）
- `deadline`：期望上线日期或里程碑
- `acceptance`：验收标准，至少两条可测试的条件
- `feasibility`：技术可行性初判（已评估 / 待评估 / 存在风险）

## 优先级定义

- **P0**：阻塞核心业务流程或有外部承诺的截止日期，需立即响应
- **P1**：重要功能，影响用户体验或业务指标，本迭代必须完成
- **P2**：优化型需求，不影响主流程，可排入下个迭代或按资源决定

## AI 处理规则

1. 收到非结构化需求时，**必须先调用** `intake-requirement` 技能进行结构化
2. 结构化完成后，**主动列出**所有缺失或模糊的字段，并向提需方澄清
3. 未获得澄清确认前，**不得**生成技术方案或开发计划
4. 需求变更时，**必须**更新原需求文档并记录变更原因和时间

## 需求文档存放规范

```
docs/
└── requirements/
    ├── backlog/          # 待评估需求
    ├── approved/         # 已确认、待开发需求
    ├── in-progress/      # 开发中
    └── done/             # 已交付
```
