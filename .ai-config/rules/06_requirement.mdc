# 需求接入规范

> 字段的机械校验规则定义在 `.ai-config/workflow.md` 第 3.1 节和 `.ai-config/scripts/validate-doc.js` 的 SCHEMAS.requirement。
> 本文件只谈人工理解层面的规范。

## 核心原则

除 XS 级外，所有需求在进入开发流程前必须完成标准化接入；不允许基于口头描述或非结构化文本直接开始开发。

## 需求来源分类

| 来源类型 | 典型形式 | 使用模板 |
|---|---|---|
| 内部研发自驱 | 技术优化、重构、新特性提案 | `from_prd.md` |
| 运营/业务方 | 飞书消息、口头描述、会议记录 | `from_verbal.md` |

## 按工作量等级的必填字段

### XS 级（≤ 30 分钟改动，快车道）

**不走 `/intake` 流程**。允许直接在 git 上操作：

```
commit message: XS: 调登录页文案
PR 描述: 简单说明改动 + 自测截图
```

不产生 `docs/requirements/backlog/*.md`。commit-msg hook 会校验 `XS:` 前缀是否合法。

### S 级（6 个核心字段）

走 `/intake`，必填：`need_id / title / workload / priority / deadline / goal / acceptance≥1 / stage / status`

### M 级（补充 4 个业务字段）

S 的全部 + `background / scope / tech_constraints`，`acceptance≥2`

### L 级（补充 4 个方案字段）

M 的全部 + `stakeholders / non_functional / risks / tech_sketch`，`acceptance≥3`

### XL 级（L 的全部 + 迭代拆分）

L 的全部 + `iteration_plan / milestones`

## 优先级定义

- **P0**：阻塞核心业务流程或有外部承诺的截止日期，需立即响应
- **P1**：重要功能，影响用户体验或业务指标，本迭代必须完成
- **P2**：优化型需求，不影响主流程，可排入下个迭代或按资源决定

## AI 处理规则

1. 收到非结构化需求时先评估等级。若是 XS：直接指引 git 快车道，不走 `/intake`
2. 其他等级：调用 `intake-requirement` skill 结构化
3. 主动列出缺失或模糊字段，向提需方澄清
4. 未澄清前，不生成技术方案或开发计划
5. 需求变更必须更新原文档并记录原因和时间

## 需求文档存放规范

```
docs/
└── requirements/
    ├── backlog/          # 待开发需求（/intake 写入，XS 不写入）
    └── done/             # 已交付需求（/deliver 归档）
```
