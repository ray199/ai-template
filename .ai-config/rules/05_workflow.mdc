# 工作流程规范

## 核心工作流程

### 流程概览
```
需求输入（口头 / 飞书 / PRD）
     ↓
[阶段0：需求接入与结构化]          ← skill: intake-requirement
     ↓ 人工确认：需求文档完整、字段无缺失
[阶段1：需求评估]                   ← skill: evaluate-requirement
     ├─ 伪需求扫描（结合代码库/设计文档/历史需求）
     └─ 输出物推荐（spec / 原型 / 迭代计划）
     ↓ 人工确认：评估结论 + 输出物清单
[阶段1.5：按需生产输出物]           ← 按确认结果选择性执行
     ├─ Spec            skill: project-docs-standards
     ├─ UI 原型         skill: generate-prototype
     └─ 迭代计划        skill: write-plan
     ↓ 人工确认：输出物内容
[阶段2：技术设计]                   ← agent: architect
     ↓ 团队确认
[阶段3：编码实现]
     ↓ 开发者确认
[阶段4：自测与验证]
     ↓ 测试确认
[阶段5：人工审查]
     ↓ 架构师确认
交付
```

### 重要原则
1. **严格顺序**：绝对不能跨步骤执行，必须完成当前阶段并获得确认后才能进入下一阶段
2. **人工确认**：每个阶段结束后必须获得相关方的人工确认
3. **文档记录**：每个阶段的成果和确认过程都需要详细记录
4. **评估优先**：所有需求在进入技术设计前必须通过阶段1评估，🔴 阻断项未解决时禁止继续流转
5. **输出物按需**：阶段1.5 的输出物不强制全部生产，以人工确认的清单为准；低复杂度需求可直接跳过进入阶段2

## 详细流程说明

### 阶段0：需求接入与结构化

#### 输入
- 原始需求（飞书消息 / 口头描述 / PRD 文档 / 研发提案）

#### 活动
1. 调用 `intake-requirement` 技能，识别来源类型，选择对应模板
2. 提取并填充结构化字段
3. 输出完整性检查结果和澄清问题清单
4. 等待提需方确认所有必填字段

#### 输出
- 标准化需求文档（存入 `docs/requirements/backlog/`）
- 待澄清问题清单

#### 确认标准
- 9 个必填字段全部填写完整
- 提需方对 AI 解读内容逐项确认
- 需求状态流转为 `approved`

---

### 阶段1：需求评估

#### 输入
- 已 approved 的需求文档
- 项目上下文：代码库、设计文档（`docs/`）、历史需求文档

#### 活动
1. 调用 `evaluate-requirement` 技能
2. **伪需求扫描**：按 `pseudo_checklist.md` 执行 4 类检查
   - A. 重复建设：扫描代码库和历史需求，查找相似功能
   - B. 价值存疑：核查需求文档中目标和验收标准的质量
   - C. 逻辑冲突：比对设计文档和历史决策
   - D. 技术可行性：结合技术栈约束和架构现状评估
3. **输出物推荐**：按复杂度矩阵，推荐需要生产的输出物清单
4. 生成评估报告，等待人工确认

#### 输出
- 需求评估报告（含4类扫描结论 + 输出物推荐）

#### 确认标准
- 所有 🔴 阻断项有明确处理决策（解决 / 放弃需求 / 修改需求）
- 输出物清单由人工最终确认
- 无阻断项时：需求状态保持 `approved`，继续流转
- 有阻断项且决定修改需求：退回阶段0重新结构化

---

### 阶段1.5：按需生产输出物

> 本阶段为**条件执行**，根据阶段1确认的输出物清单决定执行哪些子步骤。
> 低复杂度需求（清单为空）直接跳过，进入阶段2。

#### Spec（功能规格文档）
- 触发条件：人工确认"需要 Spec"
- 执行方式：调用 `project-docs-standards` 技能
- 内容要求：功能边界、数据结构、接口契约、异常处理、边界条件

#### UI 原型
- 触发条件：人工确认"需要原型"且涉及新页面/交互改动
- 执行方式：调用 `generate-prototype` 技能
- 内容要求：页面结构、核心交互流程、响应式布局

#### 迭代计划
- 触发条件：人工确认"需要迭代计划"（通常工作量 L/XL 或跨迭代）
- 执行方式：调用 `write-plan` 技能
- 内容要求：子任务拆分、里程碑、依赖关系、工作量估算

#### 确认标准
- 各输出物经提需方或技术负责人确认内容正确
- Spec 中无待填写的空白项
- 原型经视觉确认后方可继续

---

### 阶段2：技术设计

#### 输入
- 已确认的需求文档
- 阶段1.5 产出的 Spec / 原型 / 迭代计划（如有）

#### 活动
1. 触发 `architect` 代理
2. 架构设计：确定系统架构、模块划分
3. 技术选型：选择合适的技术栈和框架
4. 数据库设计：设计数据模型和表结构
5. API 设计：定义接口规范和调用方式
6. 制定开发计划和里程碑

#### 输出
- 技术架构文档
- 数据库设计文档
- API 接口文档
- 开发计划

#### 确认标准
- 团队评审技术方案
- 架构师确认设计可行性
- 技术文档完整且符合规范

---

### 阶段3：编码实现

#### 输入
- 技术架构文档
- 数据库设计文档
- API 接口文档
- 开发计划

#### 活动
1. 搭建开发环境
2. 实现数据库结构
3. 开发后端 API
4. 开发前端界面
5. 集成测试

#### 输出
- 源代码
- 单元测试
- 集成测试报告

#### 确认标准
- 代码符合编码规范
- 功能实现完整
- 单元测试通过

---

### 阶段4：自测与验证

#### 输入
- 源代码
- 功能需求列表
- 测试计划

#### 活动
1. 功能测试：验证所有功能点
2. 性能测试：测试系统性能
3. 安全测试：检查安全漏洞
4. 兼容性测试：验证不同环境
5. 生成测试报告

#### 输出
- 测试报告
- 缺陷清单
- 修复方案

#### 确认标准
- 测试覆盖率达到要求
- 严重缺陷已修复
- 测试报告完整

---

### 阶段5：人工审查

#### 输入
- 源代码
- 测试报告
- 技术文档

#### 活动
1. 代码审查：检查代码质量和安全性
2. 架构审查：评估架构设计合理性
3. 文档审查：检查文档完整性
4. 合规审查：确保符合相关规范

#### 输出
- 审查报告
- 改进建议
- 最终确认文档

#### 确认标准
- 架构师确认代码质量
- 所有审查问题已解决
- 符合交付标准

---

## 交付标准

### 交付物
1. 完整的源代码
2. 技术文档（架构、数据库、API）
3. 测试报告
4. 部署指南
5. 用户手册

### 质量要求
1. 功能完整：所有需求点都已实现
2. 代码质量：符合编码规范，无严重缺陷
3. 性能达标：满足性能要求
4. 安全可靠：无安全漏洞
5. 文档完整：所有必要文档齐全

---

## 变更管理

### 需求变更
1. 记录变更请求
2. 评估变更影响（需重新执行阶段1评估）
3. 更新需求文档版本号
4. 重新确认受影响的输出物
5. 调整开发计划

### 技术变更
1. 提出变更理由
2. 评估技术风险
3. 团队评审变更
4. 更新技术文档
5. 确认变更实施

---

## 沟通机制

### 会议要求
1. 需求评审会议：确认需求理解和评估结论
2. 技术评审会议：确认技术方案
3. 代码审查会议：确认代码质量
4. 测试评审会议：确认测试结果
5. 交付评审会议：确认最终交付

### 沟通工具
1. 项目管理工具：跟踪进度和任务
2. 版本控制系统：管理代码变更
3. 文档协作工具：管理文档更新（飞书文档 / Notion）
4. 即时通讯工具：日常沟通

---

**注**：本工作流程规范适用于所有项目，确保开发过程的规范化和质量控制。团队应严格遵守流程要求，确保项目顺利交付。
