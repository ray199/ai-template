# Profile：TypeScript

适用范围：以 TypeScript 为主的前端/后端项目或共享库（不限定框架）。

## 1. 语言与编译配置（强制）
- 必须开启严格类型检查（或在项目既有基础上逐步收敛到严格模式）
- 禁止在业务逻辑中扩散 `any`；如确需使用，必须在边界层集中隔离并说明原因
- 必须避免类型与运行时不一致：关键边界必须做运行时校验（例如：API 入参/出参、配置、消息体）

## 2. 工程组织（建议）
- 类型与实现同域：类型定义尽量与代码模块同目录，避免“全局 types 巨石”
- 禁止循环依赖：公共类型/工具必须下沉到稳定模块

## 3. 异步与错误处理（强制）
- Promise 必须有明确的错误处理策略（抛出/捕获/上报）
- 禁止静默吞错；必须保留可定位信息（请求上下文、关键参数的脱敏信息）

## 4. 依赖与构建（强制）
- 禁止引入重复或功能重叠严重的依赖，除非有充分理由与迁移方案
- 必须使用项目既有包管理与构建方式（npm/yarn/pnpm 等以项目实际为准）

## 5. 安全（强制）
- 禁止在代码与日志中输出密钥/Token/用户敏感信息
- 对外输入必须校验并做合理默认值/兜底

## 6. 与通用规则的关系
- 代码风格：遵循 [02_code_style.mdc](../02_code_style.mdc)
- 安全红线：遵循 [03_security.mdc](../03_security.mdc)
