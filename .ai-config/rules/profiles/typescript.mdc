# Profile：TypeScript

适用范围：以 TypeScript 为主的前端/后端项目或共享库（不限定框架；Node.js 服务端、React/Vue 前端、SDK/库）。

> **重要**：本 Profile 不锁定版本号。所有版本相关约束在进入编码阶段前，通过**自动扫描项目配置**确定，并按版本适配矩阵选择对应规则。

---

## 0. 版本与配置检测（每次编码阶段开始前自动执行）

### 检测来源（按优先级）

| 优先级 | 文件 | 读取字段 |
|---|---|---|
| 1 | `package.json` | `devDependencies.typescript`、`engines.node` |
| 2 | `tsconfig.json` | `compilerOptions.target`、`strict`、`module`、`moduleResolution` |
| 3 | `.nvmrc` / `.tool-versions` | Node.js 版本 |
| 4 | 依赖关键词 | 框架推断（见下） |

### 框架/用途推断

| 依赖关键词 | 用途 |
|---|---|
| `react` / `next` | React / Next.js 前端 |
| `vue` | Vue 前端（见 `node_vue.mdc`） |
| `@nestjs/core` | NestJS 服务端 |
| `express` / `koa` / `fastify` / `hono` | Node 服务端 |
| `zod` / `yup` / `io-ts` / `valibot` | 运行时校验库 |
| `prisma` / `drizzle-orm` / `typeorm` | ORM |

### 输出版本上下文（Context）

```
────────────────────────────────────────────
  TypeScript 版本上下文（自动检测）
────────────────────────────�