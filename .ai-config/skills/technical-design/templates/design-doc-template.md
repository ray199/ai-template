# 设计文档完整输出模板

> Step 6 输出。落地到 `docs/design/REQ-xxx-design.md`。

```markdown
# 技术设计文档

- **need_id**：REQ-XXXXXXXX
- **标题**：[需求标题]
- **设计版本**：v1.0
- **设计日期**：YYYY-MM-DD
- **设计者**：architect
- **状态**：待评审

---

## 一、架构影响分析

### 变更类型
[全新项目（从零建立）/ 新增模块 / 改造已有模块 / 跨模块联动]

### 原型/截图参考
> 若 /pg:intake 阶段有截图或原型图，在此说明UI参考来源。
[来自 docs/prototype/REQ-XXXXXXXX.html / 无]

### 现状基线（仅老项目，强制）
> schema 校验：老项目此节必填

- 涉及模块：<module A / module B ...>
- 现有行为（改动前）：<方法 / 接口 / 作业的当前输入输出语义>
- 已知坑 / 历史包袱：<从 project-map 或代码中发现，本次不绕开就会踩的>
- 本次保留 vs 重写：<逐项说明>

### 受影响模块
| 模块 | 影响程度 | 说明 |
|---|---|---|
| user 模块 | 高 | 新增接口，需调整 Service 层 |
| order 模块 | 低 | 只读依赖，无变更 |

### 技术组件决策
| 组件 | 是否引入 | 理由 |
|---|---|---|
| Redis 缓存 | ✅ 引入 | 高频查询接口，需缓存用户基础信息 |
| MQ | ❌ 不引入 | 本期为同步流程，无异步需求 |

---

## 二、数据库设计

> 规范见 `references/db-design-rules.md`

### 新增/修改表
（DDL + 索引 + 注释）

### 数据迁移脚本
（DDL / DML + 存量填充）

### 回滚方案
（如何回滚 + DDL 回滚语句）

---

## 三、接口设计

> 规范见 `references/api-design-rules.md`

### 接口清单
| 接口名称 | Method | URL | 权限 |
|---|---|---|---|

### 接口详细说明
（请求头 / 请求体 / 响应 / 错误码）

---

## 四、前端 UI 设计（若项目含前端）

### 页面清单
| 页面名称 | 路由路径 | 对应 view 文件 | 变更类型 |
|---|---|---|---|

### 组件拆分
（页面级 view + 可复用 component 树）

### 状态管理
| 数据 | 存储位置 | 理由 |
|---|---|---|

### API 调用层规划
| 前端操作 | 调用接口 | API 文件位置 |
|---|---|---|

---

## 五、关键实现路径

### 核心流程
（后端 Controller → Service → Mapper 时序；前端 用户操作 → API → 渲染时序）

### 并发场景处理

### 技术风险点
| 风险 | 概率 | 影响 | 应对方案 |
|---|---|---|---|

---

## 六、任务拆解（L/XL 强制；M 建议；XS/S 跳过）

> schema 校验本节存在性 + 表头列名。方法论见 `references/task-breakdown-methodology.md`

### 任务清单

| ID | 任务 | 依赖 | 验收对应 | 预估 | 备注 |
|---|---|---|---|---|---|
| T1 | DB migration：新增 role / role_permission 表 | — | A1 | 0.5d | 老项目保留 user_id 外键 |
| T2 | RoleRepository + Unit Test | T1 | A1 | 1d | |
| T3 | RoleService 业务逻辑 + 缓存 | T2 | A1, A2 | 1.5d | Redis key 设计 |
| T4 | UserController /users/{id}/roles 接口 | T3 | A2 | 0.5d | |
| T5 | 前端权限组管理页面 | T3（接口契约定稿即可） | A3 | 2d | 可与 T4 并行 |
| T6 | 集成测试 + e2e | T4, T5 | A1-A4 | 1d | |

**关键路径**：T1 → T2 → T3 → T4 → T6（共 4.5 天）
**并行机会**：T5 在 T3 接口契约定稿后即可启动，与 T4 并行
**owner 建议**（可选）：后端 T1-T4，前端 T5，全栈 T6

### 重构 / 迁移顺序（仅重构 / 改造类需求）

```
1. <步骤 1>，可独立合并：是 / 否
2. <步骤 2>，可独立合并：是 / 否
...
```

**回滚边界**：在第 N 步之前可一键回滚，第 N 步之后只能前滚

---

## 七、待评审确认项

- [ ] 数据库方案是否符合 DBA 规范
- [ ] 接口版本号是否与现有规划一致
- [ ] 前端页面清单是否覆盖了所有验收场景（若含前端）
- [ ] 任务拆解的依赖与并行性合理（L/XL）
- [ ] 重构 / 迁移顺序的回滚边界明确（涉及重构时）
- [ ] [其他需要确认的决策点]

确认无误后，执行：`/pg:code REQ-XXXXXXXX`
```
